#
# 個人用の zimfw モジュール設定。
#
# ~/.zimrc は install.sh を実行するたびに上書きされるので、
# 追加したいモジュールはこのファイルに書く。このファイルは上書きされない。
#
# 書き方は ~/.zimrc と同じ。変更すると次のシェル起動時に反映される
# (モジュールの取得にはネットワークが要る)。
#
# 例:
#   zmodule romkatv/powerlevel10k
